[package]
name = "dylib_link_demo"
version = "0.1.0"
edition = "2018"
# build = "src/build.rs"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
add_lib = { version = "0.1", path = "./add_lib" }